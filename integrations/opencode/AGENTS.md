# OpenCode harness instructions

Operate BurpOllama via `burpollama` and the localhost API. Treat scope imports as
advisory until the live bounty policy has been manually confirmed. Never infer
permission from a public hostname or bug-bounty listing.

